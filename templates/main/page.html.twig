{% extends 'base.html.twig' %}

{% block title %}White theme
{% endblock %}

{% block body %}
  <div class="cont\ainer mt-4 mx-auto">
    {% if page != null %}
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">{{ page.title }}</h5>
          <p class="card-text">{{ page.introduction }}</p>
        </div>
      </div>
    {% else %}
      <div class="d-flex justify-content-center">
        <h1>White theme</h1>
      </div>
    {% endif %}
  </div>
{% endblock %}
